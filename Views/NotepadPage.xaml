<Page x:Class="WPFOfficeProject.Views.NotepadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFOfficeProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NotepadPage">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 📌 메모 목록 (좌측) -->
        <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
            <ListBox Grid.Column="0" ItemsSource="{Binding Notes}"
                     SelectedItem="{Binding SelectedNote, Mode=TwoWay}"
                     DisplayMemberPath="Title"/>
        </Border>

        <!-- 📌 메모 편집 영역 (우측) -->
        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 📝 제목 입력 필드 -->
            <TextBox Grid.Row="0"
                     Text="{Binding SelectedNote.Title, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="16" Padding="5" Margin="0,0,0,5"
                     BorderBrush="DarkGray" BorderThickness="1"/>

            <!-- 📝 본문 입력 필드 -->
            <TextBox Grid.Row="1"
                     Text="{Binding SelectedNote.Content, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     BorderBrush="DarkGray" BorderThickness="1"
                     Margin="0,0,0,5"
                     Height="300"/>

            <!-- 📌 버튼 영역 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,10,5,0">
                <Button Content="새 메모" Command="{Binding NewNoteCommand}" Margin="5"/>
                <Button Content="저장" Command="{Binding SaveCommand}" Margin="5"/>
                <Button Content="삭제" Command="{Binding DeleteNoteCommand}" Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

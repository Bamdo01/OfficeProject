<Page x:Class="WPFOfficeProject.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFOfficeProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChatPage">

    <Grid Background="#2A2A2A">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>

        <!-- 왼쪽 패널 (대화 목록) -->
        <Grid Grid.Column="0" Background="#2F2F2F">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="대화 찾기" Margin="10" Height="30"
                    Background="#3A3A3A" Foreground="White" BorderBrush="#4A4A4A"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="5">
                    <Button Content="채팅 1" Margin="5" Background="#3A3A3A" Foreground="White"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- 오른쪽 채팅창 -->
        <Grid Grid.Column="1" Background="#2A2A2A">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 채팅 내용 -->
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Messages}" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="10" Margin="5" MaxWidth="400" Background="#3D3D3D">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMine}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Background" Value="#4A4A4A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Content}" Foreground="White" TextWrapping="Wrap"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 메시지 입력 -->
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Height="40" Padding="10"
                         Background="#3A3A3A" Foreground="White"
                         BorderBrush="#4A4A4A" BorderThickness="1"
                         VerticalAlignment="Center"
                         Text="{Binding NewMessage, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1" Content="전송" Width="80" Height="40" Margin="10,0,0,0"
                        Background="#4A4A4A" Foreground="White"
                        Command="{Binding SendCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
